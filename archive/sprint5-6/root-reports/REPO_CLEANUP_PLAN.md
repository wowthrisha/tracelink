# Repository Cleanup Plan — Phase 5
**Date:** 2026-06-24  
**Sprint:** Repository Cleanup (Phase 5 of 7)

---

## Current State Problems

1. **Root cluttered with ~35 markdown files** — mix of active docs, historical snapshots, and prior-project-name files. No clear hierarchy.
2. **docs/ and frontend/docs/ are two parallel doc trees** — the Sprint 2–3 `docs/` subtree is obsolete; all active docs live in `frontend/docs/`.
3. **SQLite test databases scattered at root and backend/** — gitignored but pollute working directory.
4. **frontend/docs/certification/ holds Sprint 4.4 snapshots** — directory name implies current certification; content is from two sprints ago.
5. **27 staged deletions never committed** — `ACTION_*_DESIGN.md` and similar files deleted from disk but not committed.
6. **TRACEVIEW_* files at root** — wrong project name, creates branding inconsistency.

---

## Proposed Changes

### Change 1: Commit Pending Deletions
**Type:** LOW-RISK  
**Scope:** git commit only

Commit the 27 already-staged deletions (`ACTION_*_DESIGN.md`, enterprise reports). No files are created or modified.

---

### Change 2: Create archive/ and Execute All Moves
**Type:** LOW-RISK  
**Scope:** 78 files moved (not deleted); history preserved via git mv

Create `archive/` with 6 subdirectories. Move all ARCHIVE candidates per ARCHIVE_PLAN.md.

```
archive/
├── README.md
├── legacy-traceview/     (9 files)
├── root-historical/      (9 files)
├── browser-audit-screenshots/ (6 files)
├── docs-sprint2-3/       (25 files)
├── sprint4-4-certification/ (14 files)
└── sprint3-4-reports/    (15 files)
```

After this move:
- `docs/` at root will be empty and can be removed
- `audit_artifacts/screenshots/` will be empty and can be removed
- `frontend/docs/certification/` will be empty and can be removed

---

### Change 3: Remove Gitignored Artifacts
**Type:** LOW-RISK  
**Scope:** 24 files removed; all gitignored, all regenerated on next test run

```bash
rm -f .DS_Store
rm -f test_*.db backend/test_*.db
rm -f backend/.coverage
rm -rf tests_e2e/.pytest_cache
```

---

### Change 4: Consolidate Docs Tree
**Type:** NOT EXECUTED in Phase 6  
**Reason:** Structural consolidation (moving `frontend/docs/` → `docs/`) would break external links and is beyond LOW-RISK scope. Document as future recommendation only.

**Future recommendation:** After cleanup settles, move `frontend/docs/` to `docs/` at the root. The `frontend/` path is misleading for backend, security, and governance documentation.

---

## Repository Root After Cleanup

Expected root-level markdown files after cleanup (15 vs ~35 now):

```
README.md                       ← project overview
ARCHITECTURE_DECISIONS.md       ← architecture ADR
CHANGELOG_ENTERPRISE.md         ← changelog
FINAL_PRELAUNCH_AUDIT.md        ← production audit
HARDENING_REPORT.md             ← Sprint 5.3 hardening
IMPLEMENTATION_PROGRESS.md      ← progress tracker
P0_REVALIDATION_REPORT.md       ← P0 revalidation
PHASE_E2_SCALABILITY_AUDIT.md   ← scalability audit
PRE_PILOT_CERTIFICATION_REPORT.md ← certification
RELEASE_BLOCKERS.md             ← active blockers
RISK_REGISTER.md                ← root risk register
SECRET_ROTATION_RUNBOOK.md      ← operational runbook
SECUREDOC_CURRENT_STATE_REPORT.md ← current state
SECURITY_AUDIT_REPORT.md        ← security baseline
SYSTEM_DESIGN_REVIEW.md         ← system design
TECHNICAL_DEBT_REGISTER.md      ← debt tracker
```

Plus: `Makefile`, `docker-compose.yml`, `start.sh`, `.gitignore`, `traceview.code-workspace`  
Plus: `REPO_INVENTORY.md`, `STALE_ARTIFACTS_REPORT.md`, `DELETION_SAFETY_REPORT.md`, `ARCHIVE_PLAN.md`, `REPO_CLEANUP_PLAN.md` (this session's artifacts)

---

## Commit Plan

| Commit | Scope | Description |
|--------|-------|-------------|
| 1 | root | `refactor(repo): commit pending staged deletions (ACTION_*_DESIGN, enterprise reports)` |
| 2 | archive | `refactor(repo): archive historical reports to archive/` |
| 3 | cleanup | `refactor(repo): remove gitignored test artifacts and OS files` |

---

## Risk Assessment

| Change | Risk | Reversible |
|--------|------|-----------|
| Commit staged deletions | None — already deleted from disk | Via git revert |
| Archive moves (git mv) | None — history preserved | Via git mv back |
| Delete gitignored files | None — regenerated on next run | Regenerated by pytest |
| docs/ consolidation (NOT doing) | Medium — external link breakage | Via git mv back |

**Overall risk: LOW**
